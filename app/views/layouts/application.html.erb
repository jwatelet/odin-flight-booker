<!DOCTYPE html>
<html>
  <head>
    <title>OdinFlightBooker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="grid">
      <header>
        <%= link_to(root_path, class: "home") do %>
          <%= image_tag 'airplane.svg', class: "plane" %>
          <span>Flight Booker</span>
        <% end %>
      </header>
      <div class="container">
        <%= yield %>
      </div>
      <footer>
        <div class="copyrights"><%= Time.now.year %> Flake The Mongose ©</div>
      </footer>
    </div>
  </body>
</html>
